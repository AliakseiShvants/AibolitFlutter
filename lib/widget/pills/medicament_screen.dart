import 'package:flutter/widgets.dart';

class MedicamentScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('I am pills screen'),);
  }
}
